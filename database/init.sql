-- Smart Boda MVP1 -- initial schema scaffold
-- Alembic manages real table changes from here on;
-- this file only guarantees the database itself exists cleanly.

CREATE SCHEMA IF NOT EXISTS smart_boda;