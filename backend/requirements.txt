fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic==2.9.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
requests==2.32.3

# AUDIT FIX (MVP0 §07, Blocking): pytest was never listed even though every test file in
# backend/tests/ assumes it. httpx is required by FastAPI's TestClient in modern Starlette.
pytest==8.3.3
httpx==0.27.2